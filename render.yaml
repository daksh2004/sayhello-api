services:
  - type: web
    name: sayhello-api
    env: java
    plan: free
    buildCommand: ./mvnw clean package -DskipTests
    startCommand: java -jar target/*.jar
    envVars:
      - key: JDK_VERSION
             value:17